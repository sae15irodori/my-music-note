<div class="container">
  <div class="row">
    <div class="col-12 main">
      <span class="notice"><%= flash[:notice] %></span>
      <h1>ノート検索結果<span class="ml-3" style="font-size: 1.5rem">(全<%= @results.count %>件)</span></h1>
      <%= search_form_for @q, url: search_notes_path, class: "form-inline" do |f| %>
        <%= f.search_field :title_or_body_cont, placeholder: "ノート検索",class: "form-control rounded-pill" %>
        <%= button_tag type: "submit", class: "btn btn-default　rounded-pill" do %>
          <i class="fas fa-search"></i>
        <% end %>
      <% end %>

      <% if @results.present? %>
        <div id="main-note">
          <% @results.each do |note| %>
              <%= render '/public/shared/notes_index', note: note %>
          <% end %>
        </div>
      <% else %>
        一致する結果はありませんでした。o○
      <% end %>
    </div>
  </div>
</div>