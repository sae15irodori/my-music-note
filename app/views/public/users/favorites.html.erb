<div class="container">
  <div class="row">
    <div class="col-12 main">
      <h1>♡Notes<span class="ml-1" style="font-size: 1.5rem">♥をしたノート</span></h1>

      <% if @favorite_notes.present? %>
          <% @favorite_notes.each do |note|%>
            <% if note.user.is_deleted == false %>
              <div class="note-contents note-contents2">
                <%= link_to user_path(note.user.id) do %>
                  <%= render '/layouts/user_info', user: note.user %><!--投稿ﾕｰｻﾞｰ情報-->
                <% end %>
                <%= render '/public/shared/note_index', note: note %><!--投稿一覧-->
              </div>
            <% else %>
              <div class="note-contents note-contents2">
                この投稿のユーザーは退会済です
              </div>
            <% end %>
          <% end %><!--each-->
          <%= paginate @favorite_notes, theme: 'twitter-bootstrap-4' %>
      <% else %>
        <p>いいねしたノートはありません</p>
      <% end %><!--いいねしたﾉｰﾄあるか-->
    </div>
  </div>
</div>